# Capability Parity

## IDE actions (6)

`codex.addToThread`, `codex.addFileToThread`, `codex.newChat`, `codex.newCodexPanel`, `codex.openCommandMenu`, `codex.openSidebar`

## Slash commands (22)

See `SlashCommandRegistry.ALL`.

Disabled without public contract: `/cloud`, `/cloud-environment`, `/worktree`.
Disabled pending semantic trace: `/side`.
Experimental-gated: `/plan`, `/memories`.

Disabled routes never fall back to raw prompt injection.
